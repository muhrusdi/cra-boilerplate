import React from "react";
import { useOvermind } from "overmind-state";
import { Button } from "antd"

const Home = () => {
  // const { state, actions } = useOvermind();

  return (
    <div>
      <div>asdf</div>
      <Button>atesddd</Button>
    </div>
  );
};

export default Home;
